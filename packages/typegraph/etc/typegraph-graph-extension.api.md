## API Report File for "@nicia-ai/typegraph"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public (undocumented)
const ALL_META_EDGE_NAMES: readonly ["subClassOf", "broader", "narrower", "relatedTo", "equivalentTo", "sameAs", "differentFrom", "disjointWith", "partOf", "hasPart", "inverseOf", "implies"];

// @public
export const CURRENT_GRAPH_EXTENSION_VERSION: 1;

// @public
export function defineGraphExtension(input: GraphExtension): GraphExtension;

// @public
type ErrorCategory = "user" | "constraint" | "system";

// @public
export type ExtensionArrayItemType = ExtensionStringProperty | ExtensionNumberProperty | ExtensionBooleanProperty | ExtensionEnumProperty | ExtensionObjectProperty;

// @public
export type ExtensionArrayProperty = Readonly<{
    type: "array";
    items: ExtensionArrayItemType;
}> & ExtensionPropertyModifiers;

// @public
export type ExtensionBooleanProperty = Readonly<{
    type: "boolean";
}> & ExtensionPropertyModifiers;

// @public
export type ExtensionEdgeDef = Readonly<{
    description?: string;
    annotations?: KindAnnotations;
    from: readonly string[];
    to: readonly string[];
    properties?: Readonly<Record<string, ExtensionPropertyType>>;
}>;

// @public
export type ExtensionEdgeIndex = Readonly<{
    entity: "edge";
    kind: string;
    name?: string;
    direction?: "out" | "in" | "none";
    fields: readonly string[];
    coveringFields?: readonly string[];
    unique?: boolean;
    scope?: "graphAndKind" | "graph" | "none";
    where?: ExtensionIndexWhere;
}>;

// @public
export type ExtensionEmbeddingModifier = Readonly<{
    dimensions: number;
}>;

// @public
export type ExtensionEnumProperty = Readonly<{
    type: "enum";
    values: readonly string[];
}> & ExtensionPropertyModifiers;

// @public (undocumented)
export type ExtensionIndex = ExtensionNodeIndex | ExtensionEdgeIndex;

// @public
export type ExtensionIndexWhere = Readonly<{
    field: string;
    op: NullCheckOp;
}>;

// @public
export type ExtensionNodeDef = Readonly<{
    description?: string;
    annotations?: KindAnnotations;
    properties: Readonly<Record<string, ExtensionPropertyType>>;
    unique?: readonly ExtensionUniqueConstraint[];
}>;

// @public
export type ExtensionNodeIndex = Readonly<{
    entity: "node";
    kind: string;
    name?: string;
    fields: readonly string[];
    coveringFields?: readonly string[];
    unique?: boolean;
    scope?: "graphAndKind" | "graph" | "none";
    where?: ExtensionIndexWhere;
}>;

// @public
export type ExtensionNumberProperty = Readonly<{
    type: "number";
    min?: number;
    max?: number;
    int?: boolean;
}> & ExtensionPropertyModifiers;

// @public
export type ExtensionObjectFieldProperty = ExtensionStringProperty | ExtensionNumberProperty | ExtensionBooleanProperty | ExtensionEnumProperty | ExtensionArrayProperty;

// @public
export type ExtensionObjectProperty = Readonly<{
    type: "object";
    properties: Readonly<Record<string, ExtensionObjectFieldProperty>>;
}> & ExtensionPropertyModifiers;

// @public
export type ExtensionOntologyRelation = Readonly<{
    metaEdge: MetaEdgeName;
    from: string;
    to: string;
}>;

// @public
type ExtensionPropertyModifiers = Readonly<{
    optional?: boolean;
    searchable?: ExtensionSearchableModifier;
    embedding?: ExtensionEmbeddingModifier;
    description?: string;
}>;

// @public
export type ExtensionPropertyType = ExtensionStringProperty | ExtensionNumberProperty | ExtensionBooleanProperty | ExtensionEnumProperty | ExtensionArrayProperty | ExtensionObjectProperty;

// @public
export type ExtensionSearchableModifier = Readonly<{
    language?: string;
}>;

// @public
export type ExtensionStringProperty = Readonly<{
    type: "string";
    minLength?: number;
    maxLength?: number;
    pattern?: string;
    format?: "datetime" | "uri" | "email" | "uuid" | "date";
}> & ExtensionPropertyModifiers;

// @public
export type ExtensionUniqueConstraint = Readonly<{
    name: string;
    fields: readonly string[];
    scope?: "kind" | "kindWithSubClasses";
    collation?: "binary" | "caseInsensitive";
    where?: ExtensionUniqueWhere;
}>;

// @public
export type ExtensionUniqueWhere = Readonly<{
    field: string;
    op: NullCheckOp;
}>;

// @public
export const GRAPH_EXTENSION_ISSUE_CODES: readonly ["UNSUPPORTED_PROPERTY_TYPE", "INVALID_PROPERTY_REFINEMENT", "NESTED_ARRAY", "NESTED_OBJECT_TOO_DEEP", "INVALID_MODIFIER_TARGET", "INVALID_ENUM_VALUES", "INVALID_NUMBER_BOUNDS", "INVALID_LENGTH_BOUNDS", "INVALID_PATTERN", "INVALID_EMBEDDING_DIMENSIONS", "INVALID_SEARCHABLE_LANGUAGE", "RESERVED_PROPERTY_NAME", "INVALID_KIND_NAME", "DUPLICATE_KIND_NAME", "EMPTY_PROPERTIES", "EMPTY_FROM_OR_TO", "DUPLICATE_UNIQUE_CONSTRAINT", "EMPTY_UNIQUE_FIELDS", "DUPLICATE_UNIQUE_FIELD", "UNKNOWN_UNIQUE_FIELD", "INVALID_UNIQUE_WHERE_OP", "UNKNOWN_UNIQUE_WHERE_FIELD", "INVALID_ANNOTATION", "UNKNOWN_META_EDGE", "ONTOLOGY_CYCLE", "ONTOLOGY_SELF_LOOP", "ONTOLOGY_DISJOINT_CONFLICT", "DUPLICATE_ONTOLOGY_RELATION", "INVALID_DOCUMENT_SHAPE", "UNKNOWN_DOCUMENT_KEY", "UNSUPPORTED_STRING_FORMAT", "INVALID_INDEX_DECLARATION", "DUPLICATE_INDEX_NAME", "EMPTY_INDEX_FIELDS", "UNKNOWN_PROPERTY_KEY"];

// @public
export const GRAPH_EXTENSION_TOP_LEVEL_KEYS: readonly ["version", "nodes", "edges", "ontology", "indexes"];

// @public
export type GraphExtension = Readonly<{
    version?: GraphExtensionVersion;
    nodes?: Readonly<Record<string, ExtensionNodeDef>>;
    edges?: Readonly<Record<string, ExtensionEdgeDef>>;
    ontology?: readonly ExtensionOntologyRelation[];
    indexes?: readonly ExtensionIndex[];
}>;

// @public
export abstract class GraphExtensionError extends TypeGraphError {
    protected constructor(spec: {
        message: string;
        code: string;
        details: Readonly<Record<string, unknown>>;
        suggestion: string;
        category?: ErrorCategory;
        cause?: unknown;
    });
    // (undocumented)
    readonly code: string;
}

// @public
export type GraphExtensionIssue = Readonly<{
    path: string;
    message: string;
    code: GraphExtensionIssueCode;
}>;

// @public (undocumented)
export type GraphExtensionIssueCode = (typeof GRAPH_EXTENSION_ISSUE_CODES)[number];

// @public (undocumented)
export type GraphExtensionTopLevelKey = (typeof GRAPH_EXTENSION_TOP_LEVEL_KEYS)[number];

// @public
export class GraphExtensionUnresolvedEndpointError extends GraphExtensionError {
    constructor(edgeKind: string, side: "from" | "to", endpoint: string, graphId: string);
    // (undocumented)
    readonly code: "GRAPH_EXTENSION_UNRESOLVED_ENDPOINT";
    // (undocumented)
    readonly edgeKind: string;
    // (undocumented)
    readonly endpoint: string;
    // (undocumented)
    readonly side: "from" | "to";
}

// @public
export class GraphExtensionValidationError extends GraphExtensionError {
    constructor(issues: readonly GraphExtensionIssue[], cause?: unknown);
    // (undocumented)
    readonly code: "GRAPH_EXTENSION_INVALID";
    // (undocumented)
    readonly details: Readonly<{
        issues: readonly GraphExtensionIssue[];
    }>;
    // (undocumented)
    readonly issues: readonly GraphExtensionIssue[];
}

// @public
export type GraphExtensionVersion = number;

// @public
export class GraphExtensionVersionUnsupportedError extends GraphExtensionError {
    constructor(persistedVersion: number, currentVersion: number);
    // (undocumented)
    readonly code: "GRAPH_EXTENSION_VERSION_UNSUPPORTED";
    // (undocumented)
    readonly currentVersion: number;
    // (undocumented)
    readonly persistedVersion: number;
}

// @public
export const INCOMPATIBLE_CHANGE_TYPES: readonly ["REMOVE_PROPERTY", "ADD_REQUIRED_PROPERTY", "TIGHTEN_OPTIONALITY", "TIGHTEN_CONSTRAINT", "ADD_PATTERN", "CHANGE_PATTERN", "ADD_FORMAT", "CHANGE_FORMAT", "TIGHTEN_INT", "TIGHTEN_ENUM", "TYPE_CHANGE", "ADD_UNIQUE_ON_POPULATED", "TIGHTEN_EDGE_ENDPOINTS"];

// @public (undocumented)
export type IncompatibleChange = Readonly<{
    kind: string;
    field?: string;
    type: IncompatibleChangeType;
    detail?: string;
}>;

// @public
export class IncompatibleChangeError extends GraphExtensionError {
    constructor(changes: readonly IncompatibleChange[], graphId: string);
    // (undocumented)
    readonly changes: readonly IncompatibleChange[];
    // (undocumented)
    readonly code: "INCOMPATIBLE_CHANGE";
}

// @public (undocumented)
export type IncompatibleChangeType = (typeof INCOMPATIBLE_CHANGE_TYPES)[number];

// @public
type JsonValue = null | string | number | boolean | readonly JsonValue[] | Readonly<{
    [key: string]: JsonValue;
}>;

// @public
type KindAnnotations = Readonly<Record<string, JsonValue>>;

// @public
export class KindCollisionError extends GraphExtensionError {
    constructor(kindName: string, entity: KindEntity, graphId: string);
    // (undocumented)
    readonly code: "KIND_COLLISION";
    // (undocumented)
    readonly entity: KindEntity;
    // (undocumented)
    readonly kindName: string;
}

// @public
type KindEntity = "node" | "edge";

// @public
export class KindHasReferentsError extends GraphExtensionError {
    constructor(kindName: string, referents: readonly KindReferent[], graphId: string);
    // (undocumented)
    readonly code: "KIND_HAS_REFERENTS";
    // (undocumented)
    readonly kindName: string;
    // (undocumented)
    readonly referents: readonly KindReferent[];
}

// @public (undocumented)
export type KindReferent = Readonly<{
    type: "compile-time-edge" | "compile-time-ontology";
    name: string;
}>;

// @public
export const LEGACY_GRAPH_EXTENSION_VERSION: 1;

// @public (undocumented)
type MetaEdgeName = (typeof ALL_META_EDGE_NAMES)[number];

// @public
type NullCheckOp = "isNull" | "isNotNull";

// @public
export class RemoveCompileTimeKindError extends GraphExtensionError {
    constructor(kindName: string, entity: KindEntity, graphId: string);
    // (undocumented)
    readonly code: "REMOVE_COMPILE_TIME_KIND";
    // (undocumented)
    readonly entity: KindEntity;
    // (undocumented)
    readonly kindName: string;
}

// @public
type Result<T, E = Error> = Readonly<{
    success: true;
    data: T;
}> | Readonly<{
    success: false;
    error: E;
}>;

// @public
class TypeGraphError extends Error {
    constructor(message: string, code: string, options: TypeGraphErrorOptions);
    readonly category: ErrorCategory;
    readonly code: string;
    readonly details: Readonly<Record<string, unknown>>;
    readonly suggestion?: string;
    toLogString(): string;
    toUserMessage(): string;
}

// @public
type TypeGraphErrorOptions = Readonly<{
    details?: Record<string, unknown>;
    category: ErrorCategory;
    suggestion?: string;
    cause?: unknown;
}>;

// @public
export function validateGraphExtension(input: unknown, options?: ValidateGraphExtensionOptions): Result<GraphExtension, GraphExtensionValidationError>;

// @public (undocumented)
type ValidateGraphExtensionOptions = Readonly<{
    strict?: boolean;
}>;

// (No @packageDocumentation comment for this package)

```
