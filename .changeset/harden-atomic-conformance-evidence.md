---
"@nicia-ai/typegraph": minor
---

Strengthen custom-backend atomic conformance so the runners bind the exact registered transport, observe semantic dispatch inside registered executors, probe author-created derived backends, report inapplicable transaction checks as skipped, validate all bindings before fixture preparation, and distinguish native from pre-dispatch semantic refusals. Bundled libSQL now self-certifies every reachable semantic mutation variant against committed database state.
